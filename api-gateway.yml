server:
  port: 8088

eureka:
    instance:
      hostname: service-discovery
client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://service-discovery:8761/eureka

