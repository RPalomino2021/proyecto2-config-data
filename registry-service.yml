server:
  port: 8761
  
eureka:
  instance:
    hostname: service-discovery
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://service-discovery:${server.port}/eureka/

spring.cloud.config.discovery.enabled: true
