server:
  port: 8761

eureka:
  instance:
    hostname: service-discovery
client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://service-discovery:8761/eureka
