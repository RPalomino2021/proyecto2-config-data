server:
  port: 8761
  
eureka:
  instance:
    hostname: registry-service
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://registry-service:${server.port}/eureka/

spring.cloud.config.discovery.enabled: true
